import { ExecuteParameters, ExecuteResponse, Notification, VerbId } from '../../JsApiInternalContract';
import { ExternalContractVersionConverter } from './ExternalContractVersionConverter';
/**
 * This version converter doesn't actually do anything but is useful for testing or when we have
 * a matching platform and external version number
 */
export declare class ExternalIdentityVersionConverter implements ExternalContractVersionConverter {
    downgradeExecuteCall(verb: any, parameters: any): {
        verb: VerbId;
        parameters: ExecuteParameters;
    };
    upgradeExecuteReturn(executeResponse: ExecuteResponse, verb: VerbId, parameters: ExecuteParameters): ExecuteResponse;
    upgradeNotification(notification: Notification): Notification;
}
//# sourceMappingURL=ExternalIdentityVersionConverter.d.ts.map