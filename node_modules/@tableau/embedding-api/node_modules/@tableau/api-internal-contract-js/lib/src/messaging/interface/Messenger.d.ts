import { MessageDispatcher } from './MessageDispatcher';
import { MessageListener } from './MessageListener';
/**
 * Simple wrapper interface to encompass both the MessageDispatcher and MessageListener
 */
export interface Messenger extends MessageDispatcher, MessageListener {
}
//# sourceMappingURL=Messenger.d.ts.map