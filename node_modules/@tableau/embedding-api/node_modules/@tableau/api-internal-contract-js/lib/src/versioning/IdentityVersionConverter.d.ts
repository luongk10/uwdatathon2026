import { ExecuteParameters, ExecuteResponse, Notification, VerbId } from '../JsApiInternalContract';
import { InternalContractVersionConverter } from './InternalContractVersionConverter';
/**
 * This version converter doesn't actually do anything but is useful for testing or when we have
 * a matching platform and internal version number
 */
export declare class IdentityVersionConverter implements InternalContractVersionConverter {
    upgradeExecuteCall(verb: any, parameters: any): {
        verb: VerbId;
        parameters: ExecuteParameters;
    };
    downgradeExecuteReturn(executeResponse: ExecuteResponse): ExecuteResponse;
    downgradeNotification(notification: Notification): Notification;
}
//# sourceMappingURL=IdentityVersionConverter.d.ts.map