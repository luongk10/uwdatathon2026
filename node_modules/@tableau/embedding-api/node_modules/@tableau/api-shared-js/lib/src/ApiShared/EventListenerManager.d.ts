import * as Contract from '@tableau/api-external-contract-js';
import { TableauEventType } from '@tableau/api-external-contract-js';
import { SingleEventManager } from './SingleEventManager';
/**
 * Class designed to register and unregister handlers from a user. Only those events
 * which are added via AddNewEventType will be supported by this instance
 */
export declare class EventListenerManager implements Contract.EventListenerManager {
    private _eventListenerManagers;
    constructor();
    addEventListener(eventType: TableauEventType, handler: Contract.TableauEventHandlerFn): Contract.TableauEventUnregisterFn;
    removeEventListener(eventType: TableauEventType, handler: Contract.TableauEventHandlerFn): boolean;
    protected addNewEventType(eventManager: SingleEventManager): void;
}
//# sourceMappingURL=EventListenerManager.d.ts.map