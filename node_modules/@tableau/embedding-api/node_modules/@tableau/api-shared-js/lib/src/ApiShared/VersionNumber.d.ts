import { VersionNumberContract } from './VersionNumberContract';
/**
 * Represents the current version of the extensions, embedding
 * and extensions-api-types library
 */
export declare class VersionNumber implements VersionNumberContract {
    major: number;
    minor: number;
    fix: number;
    build: number;
    isAlpha: boolean;
    constructor(versionString: string, isAlpha: boolean);
    private getBuildNumber;
    get formattedValue(): string;
    get fullFormattedValue(): string;
}
//# sourceMappingURL=VersionNumber.d.ts.map